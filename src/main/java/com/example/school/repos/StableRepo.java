package com.example.school.repos;

import com.example.school.entities.Stable;
import org.springframework.data.jpa.repository.JpaRepository;

public interface StableRepo extends JpaRepository<Stable, Long> {
}
